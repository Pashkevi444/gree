<?php

declare(strict_types=1);

namespace Gree\DB;

/**
 * Маркер-база для инфраструктурных сервисов слоя БД (TransactionService и
 * любые будущие — например, фабрика для дополнительных коннекшенов,
 * health-check сервиса, миграционный раннер и пр.).
 *
 * Тело пустое, абстрактный. Введён для единообразного типизирования
 * семейства DB-сервисов и точки для общей логики в будущем (логгер, метрики,
 * единый sql-сериализатор для отладки).
 */
abstract class BaseDbService {}
